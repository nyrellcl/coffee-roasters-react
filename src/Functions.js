import "./pages/plan/Preference/PreferenceTemplate";
import "./pages/plan/Summary"

/**To update the order summary span contents according to what the user selects
 * on the preference template.
 * If user chooses, caffeine, then that choice must reflect on the order summary, same with the next choices to complete the sentence in the order summaryAS/* ¸43 */
